# Makefile automatically generated by ghdl
# Version: GHDL 0.35-dev (tarball) [Dunoon edition] - GCC back-end code generator
# Command used to generate this makefile:
# ghdl --gen-makefile --ieee=synopsys vga_tb
SRCS := bin2height.vhd\
	bin2height_tb.vhd
OBJS := $(SRCS:vhd=o)
TARGET := bin2height_tb

GHDL=ghdl
GHDLFLAGS= --ieee=synopsys
GHDLRUNFLAGS= --wave=$(TARGET).ghw

# Default target
all: $(TARGET)

# Elaboration target
bin2height_tb: $(OBJS)
	$(GHDL) -e $(GHDLFLAGS) $@

clean:
	rm $(OBJS) $(TARGET) $(TARGET).ghw
# Run target
run: $(TARGET)
	$(GHDL) -r $(TARGET) $(GHDLRUNFLAGS)

# Targets to analyze files
%.o: %.vhd
	$(GHDL) -a $(GHDLFLAGS) $<
